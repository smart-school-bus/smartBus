<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.smartbus10">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChangePassword"></activity>
        <activity android:name=".RegistrationStudentFragment" />
        <activity android:name=".RegistrationDriverFragment" />
        <activity android:name=".RegistrationParentFragment" />
        <activity android:name=".Registration" />
        <activity
            android:name=".PersonList"
            android:label=""
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Home"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ParentList" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".SignInPage"
            android:label="@string/title_activity_sign_in_page"
            android:theme="@style/AppTheme.noDarkActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.noDarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>